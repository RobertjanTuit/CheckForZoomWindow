"start": "npx pm2 kill && npx pm2 resurrect && npm run stop & npx pm2 start index.js -f --watch && npm run log",
"stop": "npx pm2 delete index.js -s",
"log": "npx pm2 logs",
"test": "echo \"Error: no test specified\" && exit 1"*/
