{
  "name": "checkforzoomwindow",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "npx pm2 start index.js -f --watch && npx pm2 logs",
    "stop": "npx pm2 delete index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^3.3.2",
    "node-gyp": "^10.0.1",
    "node-window-manager": "^2.2.4",
    "pm2": "^5.3.0"
  }
}
